# AI_Invoice_OCR_ExtractionSystem
AI Invoice OCR Extraction System (Purchase Invoice → JSON) A company receives purchase bills as PDF/photo. Instead of typing details manually (supplier name, invoice number, items, tax, total), your app should read the bill automatically using Gemini API or ChatGPT API and give the output as clean JSON (so any ERP or tool can use it). 
